[{"module": "cloudbank-ags", "task": "授信合同上传规则调整——授信未通过时不上传、不作废。优先级：P1。预估工时：1天。依赖：无（授信状态查询接口已存在）。具体内容：\n1. 定位代码：仅修改 AuthFillJob.java 一个文件。方法 backfill() 中存在两条路径需修改：①降级路径（L121-128）：核心查询失败后走 applQueryTargetFacade fallback，已有 isFinalState && !checkIsApproval 判断，但拒绝时构建 status=\"04\" 加入 results 触发作废——行为错误；②主路径（L130-141）：核心查询成功后直接构建 status=\"03\" 加入 results 上传——完全缺失授信通过判断。\n2. 降级路径修改（L121-128）：将原有的 status=\"04\" 加入 results 逻辑改为 log.info + continue，不加入 results、不调用 backfillLoanAgreement()。日志内容包含协议号和 applState。\n3. 主路径修改（L130-141）：核心查询成功后，额外调用 applQueryTargetFacade.queryApplByApplNoAddUser() 获取 applState（方案A，与降级路径判断标准一致），使用 ApplStatusEnum.isFinalState() && !checkIsApproval() 判断。授信未通过时 log.info + continue；查询获客状态失败时 log.warn + continue；授信通过则保留原有上传逻辑不变。注意：CreditReqQueryOutput 仅有 status 字段无 applState，不能直接用 ApplStatusEnum 判断，必须走 applQueryTargetFacade。\n4. 关键约束：禁止修改 backfillLoanAgreement() 共用方法内部逻辑；execute() 入口方法无需改动；ApplStatusEnum 和 CreditReqQueryOutput 均为外部依赖不改。\n5. 防重处理：采用接受重复查询方案。授信拒绝记录不改变状态、不标记 remark，continue 跳过后下次定时任务会重复拾取。需在两处 continue 日志中包含 custNo、businessNo、applState，便于监控重复拾取量。上线后关注：每轮拾取拒绝记录数占比（如超30%需升级方案）。\n6. 单元测试（AuthFillJobTest.java 新增用例）：UC-003a 降级路径授信拒绝——mock contractQueryFacade 返回失败，mock applQueryTargetFacade 返回拒绝终态，验证 backfillLoanAgreement() 未被调用且 results 为空；UC-003b 主路径授信拒绝——mock contractQueryFacade 返回成功，mock applQueryTargetFacade 返回拒绝终态，验证 backfillLoanAgreement() 未被调用且 results 为空。UC-001、UC-002 为原有行为不变，已有测试可复用。"}, {"module": "cloudbank-lcs", "task": "无需改动。cloudbank-lcs 复核确认：queryCreditReq() 接口已存在且稳定（ContractQueryFacade Dubbo接口），上传逻辑不属于 LCS 模块职责，无需任何变更。"}, {"module": "cloudbank-api", "task": "无需改动。cloudbank-api 为接口定义层（Facade接口+DTO），本次需求不新增或修改对外API接口，CreditReqQueryOutput 等 DTO 无需变更。"}, {"module": "cloudbank-common", "task": "无需改动。本次需求不涉及公共枚举、DTO、工具类变更，无需新增响应码或外部接口定义。"}, {"module": "cloudbank-ods", "task": "无需改动。本次需求不涉及数据源接入、报表变更、宽表字段调整，不影响 ODS Facade 接口。"}, {"module": "cloudbank-pcs", "task": "无需改动。PCS 为支付通道系统，本次需求不涉及支付通道、二类户、鉴权签约等业务。"}, {"module": "cloudbank-partner", "task": "无需改动。复核确认：1）partner 层无 AuthFillJob 或 backfill() 的任何覆写或扩展；2）backfill() 变更不经过 partner 层任何扩展点（PartnerSignServiceImpl.uploadAgreement()、signSync() 等均在不同调用链）；3）partner 层 Job（HkRepayNotifyJob、ChangeDefaultBankcardJob）与授信合同上传流程无业务交集。"}]